package com.consulo.flightbusiness.service;

import org.junit.runner.RunWith;
import org.powermock.modules.junit4.PowerMockRunner;


@RunWith(PowerMockRunner.class)
public class LoginServiceIMPLTest {
	
	/*@Mock
	private LoginDAO loginDAO;
	
	@TestSubject
	private LoginServiceIMPL loginServiceIMPL = new LoginServiceIMPL(loginDAO); 
	
	@BeforeClass
	public static void setUpBeforeClass() throws Exception {
	}

	@AfterClass
	public static void tearDownAfterClass() throws Exception {
	}

	@Before
	public void setUp() throws Exception {		
		Whitebox.setInternalState(loginServiceIMPL, "loginDao", loginDAO);
	}

	@After
	public void tearDown() throws Exception {
	}

	@Test
	public void testTestMethod() {
		
		EasyMock.expect(loginDAO.testMethod()).andReturn("TEST METHOD");
		EasyMock.replay(loginDAO);
		Assert.assertEquals("TEST METHOD", loginServiceIMPL.testMethod());
		
	}*/

}
